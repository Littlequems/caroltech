function fakeAdd(target,label,status){const d=document.createElement('div');d.className='row';d.innerHTML='<b>'+label+' '+Math.floor(Math.random()*900+100)+'</b><span>'+status+'</span>';document.getElementById(target).prepend(d)}function pulse(id,a,b){const e=document.getElementById(id);if(e)e.textContent=Math.random()>.5?a:b}function done(msg){alert(msg||'Ação simulada com dados fictícios. Nenhuma informação real foi gravada.')}